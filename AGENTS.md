修改代码的规则：
1. 不曾明确要求修改的代码逻辑，则禁止去修改
2. 移除某代码逻辑时，务必考虑上下文，是否可以移除，避免移除后破坏了其他代码的功能
3. 修改代码后，需要对比验证修改代码前后是否丢失功能
4. 如果你对某段话不够理解，那么请你不要胡乱猜测，请尝试咨询用户，避免误解造成不可逆的损失
5. 每次必须确认你的理解是否达成共识, 只有在明确获取“批准修改”字样指令情况下方可修改代码
6. 项目 RapidNotes 是一个基于 Qt6 和 C++20 标准开发 land 的应用程序。