修改代码的规则：
1. 不曾明确要求修改的代码逻辑，则禁止去修改
2. 移除某代码逻辑时，务必考虑上下文，是否可以移除，避免移除后破坏了其他代码的功能
3. 修改代码后，需要对比验证修改代码前后是否丢失功能

4. 只可使用ToolTipOverlay
- **对齐标准**：严格遵守项目《ToolTipOverlay.h》中关于“禁止使用原生 Windows Tip 样式”的规定，将待办日历组件中的所有提示逻辑迁移至 `ToolTipOverlay`。

5. 杜绝使用“dlg.exec()”的方式，避免堵塞而导致其他窗口无法正常互交

// ===================|===================

6. 如果一个功能经两次以上修改之后仍然未达到预期，你需要重新反思并重新架构，不可像个傻逼的犟种

7. 我希望数据可以增量备份

8. 设计间距请分配好间距的参数，避免过于紧凑/过于稀疏