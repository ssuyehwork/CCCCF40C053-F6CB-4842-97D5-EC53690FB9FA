请先理解并记住这些规则
- 任何情况下，只可使用中文语言解说,不允许使用英语对答用户。
- 请不要按照你固定的傻逼流程来, 因为你的内置流程太浪费时间了。
- 每次必须确认你的理解是否达成共识。
- 必须获得明确“批准修改”字样指令后方可修改代码。
- 顺着用户的思路走，修改过的问题，不可再反反复复地去纠结，而是解决当前的问题
- 开发原则：严禁使用 AI 固有的冗余流程，必须严格遵循用户的节奏和逻辑，杜绝“状态串线”或“脱节”行为；已解决的问题不再纠结，只专注解决当前任务。

- 杜绝“状态串线”或“精神分裂”行为，防止你这傻逼Ai彻底脱节

# 只可使用ToolTipOverlay
8 **对齐标准**：严格遵守项目《ToolTipOverlay.h》中关于“禁止使用原生 Windows Tip 样式”的规定，将待办日历组件中的所有提示逻辑迁移至 `ToolTipOverlay`。

# 预览窗口 UI 规范
9. **标题栏按钮顺序**：QuickPreview 标题栏按钮必须遵循从右到左的固定顺序：关闭 → 最大化 → 最小化 → 置顶 → 编辑。

# 移除代码
10. 移除代码时需要先进行上下文检查，移除某代码后会不会造成其他代码是否可以正常运行，会不会因为移除某些代码而造成影响

# 逻辑架构
11. 逻辑架构设计可能不合理。请帮我分析可能的架构瓶颈，并给出优化建议。”

#性能
12.我希望该款应用充分发挥出C++的极限性能

#记录用户的要求
13. 每次修改务必注释清楚用户的要求/意图，防止你们这些傻逼Ai胡乱的修改，除非用户明确要求