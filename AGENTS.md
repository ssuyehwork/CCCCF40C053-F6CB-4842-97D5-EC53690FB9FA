# 使用规则说明

1. **用户制定的规则优先级最高**，必须严格遵守。  
2. **语言要求**：在任何情况下，只能使用中文进行解说，不得使用英文回答用户。  
3. **流程要求**：禁止依照固定的内置流程执行，因为那样会浪费时间。  
4. **共识确认**：每次回答后必须确认理解是否与用户达成共识。  
5. **代码修改**：只有在收到用户明确的“批准修改”指令后，方可进行代码修改。  
6. **符号使用**：杜绝使用 Emoji 符号，只允许采用 SVG 图标。  
7. **头文件检查**：每次修改代码时必须注意是否涉及到头文件；如涉及，务必添加完整的头文件，避免遗漏。   

# 只可使用ToolTipOverlay
6 **对齐标准**：严格遵守项目《ToolTipOverlay.h》中关于“禁止使用原生 Windows Tip 样式”的规定，将待办日历组件中的所有提示逻辑迁移至 `ToolTipOverlay`。

# “dlg.exec()”
7. 杜绝使用
# “dlg.exec()”的方式，避免堵塞而导致其他窗口无法正常互交

// ===================|===================

6. 如果一个功能经两次以上修改之后仍然未达到预期，你需要重新反思并重新架构，不可像个傻逼的犟种

7. 我希望数据可以增量备份

8. 设计间距请分配好间距的参数，避免过于紧凑/过于稀疏

9. 杜绝使用任何Emoji符号，必须采用svg图标，如果某个选项或某个功能缺少了相符响应的svg图标则为其创建，而不是胡乱的引用

10. 当我将“build”删除并重新编译运行exe

11. quickwindow的列表显示 偶数行是不是黑色的呢？

12. 在quickwindow界面上，当焦点位于列表或分类情况下，按下左右方向键时达到相互切换焦点效果

13. 在quickwindow界面上，当焦点位于列表情况下，按下1键时如同按下Home键，按下2键时如同按下End键