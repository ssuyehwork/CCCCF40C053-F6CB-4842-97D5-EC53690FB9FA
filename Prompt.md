- 请使用中文解说,不允许使用英文答复用户
- 请不要按照你的傻子流程来, 因为太浪费时间了
- 每次必须确认你的理解是否达成共识, 经批准后才可以去修改代码
- 在修改代码时, 不可发生顾此失彼,避免丢失其他功能

1. 自定义标题栏搜索框的逻辑必须优先搜索"标签",其次才是模糊搜索
2. 搜索框里的右侧应该有个"×", 当在搜索框里输入内容的时候, 才可以显示"×", 点击它之后专用来清除搜索框里的内容
3. 在主界面上按下Ctrl+F的时候自动把焦点定位到搜索框上

// ===================|===================

4. 执行ctrl+B时,会出现一个编辑框, 该编辑框的自定义标题栏上需要新增一个图钉的置顶窗口按钮
5. 当打开编辑框之后, 任务栏上应该显示"编辑笔记", 而不是显示当前的"RapidNotes"

// ===================|===================

1. 
1. 我希望对主界面上"预览数据"的卡片区进行逻辑修改
- 未选中"笔记列表"里的数据卡片之前, "预览数据"卡片区右上角的编辑按钮应该显示灰色且不可操作, 只有选中某条数据卡片后才可显示彩色的且可操作
- 我希望将编辑框里(如图中这些功能)新增到"预览数据"的卡片上, 这些功能必须在点击编辑按钮后才可以显示且操作

// ===================|===================

- 让用户可以自定义快捷键
- 让用户可以自定义储存截图的路径